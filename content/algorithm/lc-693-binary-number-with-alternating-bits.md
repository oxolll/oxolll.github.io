---
title: "LeetCode 693. Binary Number with Alternating Bits"
date: 2026-02-18T14:40:00+08:00
lastmod: 2026-02-18T14:40:00+08:00
difficulty: 1200
draft: false
hidden: false
# --- 目錄設定 ---
ShowToc: true
TocOpen: false
# --- 數學公式設定 ---
math: true
categories: ["Algorithm"]
tags: ["LeetCode", "Bit Manipulation", "Simulation"]
keywords: ["LC693", "1200", "Alternating Bits", "XOR"]
description: "LeetCode 第 693 題：Binary Number with Alternating Bits。檢查一個正整數的二進位表示中，相鄰位元是否總是交替出現 (0101... or 1010...)。"
---

## 📊 題目資訊
> **題目連結**：[LeetCode 693](https://leetcode.com/problems/binary-number-with-alternating-bits/)  
> **難度等級**：<span style="color: #00af9b; font-weight: bold;">Easy(N/A)</span>  
> **核心主題**：`Bit Manipulation`

---

## 📖 題目描述
<details>
<summary>🔍 <b>點擊展開題目詳情與限制</b></summary>

### 說明
給定一個正整數 `n`，檢查它的二進位表示是否為「交替位元」。
換句話說，任兩個相鄰的位元（Bit）必須擁有不同的值（即 0 旁邊一定是 1，1 旁邊一定是 0）。

### 範例
- **Input**: `n = 5` ($101_2$)
- **Output**: `true`
- **Input**: `n = 7` ($111_2$)
- **Output**: `false`
- **Input**: `n = 11` ($1011_2$)
- **Output**: `false`

### 限制條件
- $1 \le n \le 2^{31} - 1$
</details>

---

## 📝 歷次打卡與更新
- [2026-02-18：初次提交 (Iterative Bit Check)](#2026-02-18-初次提交)

---

## 💡 2026-02-18 初次提交

### 直覺 (Intuition)
題目要求相鄰位元必須不同（例如 `...10101`），這意味著我們只要檢查每一位元 (Current Bit) 是否與上一位元 (Previous Bit) 相反即可。
如果發現任何一對相鄰位元相同（例如 `00` 或 `11`），則直接回傳 `false`。

### 解題思路 (Approach)
我們可以使用迴圈配合位元運算來逐一檢查：
1.  **初始狀態**：先取出最低位元 (LSB, `n & 1`) 作為 `pre`，然後將 `n` 右移一位。
2.  **迴圈檢查**：當 `n > 0` 時，持續執行以下步驟：
    * 取出當前最低位 `cur = n & 1`。
    * 檢查 `cur` 與 `pre` 是否不同。
        * 利用 XOR 特性：若 `cur` 與 `pre` 不同，則 `cur ^ pre` 應為 `1`。
        * 若 `cur ^ pre != 1`（即相同），回傳 `false`。
    * 更新 `pre = cur`，並將 `n` 右移一位。
3.  **結束**：若迴圈跑完都沒有發現錯誤，回傳 `true`。

### 📊 複雜度分析
- **時間複雜度**: $O(\log N)$ 或 $O(1)$。
  因為 $N$ 最大為 $2^{31}-1$，二進位最多只有 31 位，迴圈次數固定在 31 次以內。
- **空間複雜度**: $O(1)$。
  僅使用常數空間儲存變數。

### 💻 程式碼實作 (C++)
```cpp
class Solution {
public:
    bool hasAlternatingBits(int n) {
        // 1. 取出最低位作為初始的前一位
        int pre = n & 1;
        n >>= 1;
        
        // 2. 逐位檢查
        while(n) {
            // 檢查當前位 (n&1) 是否與前一位 (pre) 不同
            // 若相同 (0^0=0, 1^1=0)，則 XOR 結果不為 1，回傳 false
            // 注意運算子優先順序：!= 高於 ^
            if ((pre ^ (n & 1)) != 1) 
                return false;
            
            pre = (n & 1); // 更新 pre
            n >>= 1;       // 右移處理下一位
        }
        return true;
    }
};
```

<script>
    MathJax = {
      tex: {
        inlineMath: [['$', '$'], ['\\(', '\\)']],
        displayMath: [['$$','$$'], ['\\[', '\\]']],
        processEscapes: true
      },
      options: {
        skipHtmlTags: ['script', 'noscript', 'style', 'textarea', 'pre']
      }
    };
</script>
<script async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>